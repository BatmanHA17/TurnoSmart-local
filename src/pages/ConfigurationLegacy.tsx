import { MainLayout } from "@/components/MainLayout";
import { ConfigurationHub } from "@/components/ConfigurationHub";

export default function ConfigurationLegacy() {
  return (
    <MainLayout>
      <ConfigurationHub />
    </MainLayout>
  );
}